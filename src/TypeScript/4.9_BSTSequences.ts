// 4.9 BST Sequences: A binary search tree was created by traversing through an array from left to right
// and inserting each element. Given a binary search tree with distinct elements, print all possible
// arrays that could have led to this tree.

import TreeNode from "../DataStructures/TreeNode";

const getBSTSequences = (root: TreeNode | null) => {

};

/*
Assumptions:
- Valid BST with distinct els
- 

Thoughts:
Focus on 3 node tree first and see how many possible with just 3
*/

